const Read = (props) => {
    const {id} = props;
    console.log(id)
    return (
        <div>
            <h3>
                User Details
            </h3>
        </div>
    )
}

export default Read